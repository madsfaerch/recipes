---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} — Mads' Recipes</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  </head>
  <body class="min-h-screen">
    <header class="border-b border-stone-200 bg-cream/80 backdrop-blur-sm sticky top-0 z-10">
      <nav class="max-w-3xl mx-auto px-6 py-4 flex items-center justify-between">
        <a href="/" class="font-serif text-xl font-bold text-stone-900 hover:text-spice transition-colors">
          Recipes
        </a>
      </nav>
    </header>
    <main class="max-w-3xl mx-auto px-6 py-10">
      <slot />
    </main>
    <footer class="max-w-3xl mx-auto px-6 py-8 text-center text-sm text-stone-400 border-t border-stone-200">
      Made with good taste
    </footer>
  </body>
</html>
